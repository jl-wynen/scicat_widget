{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/*.ts --minify --format=esm --bundle --outdir=src/scicat_widget/_static",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {},
	"devDependencies": {
		"@anywidget/types": "^0.2.0",
		"typescript": "^5.8.3",
		"esbuild": "^0.25.4"
	}
}
